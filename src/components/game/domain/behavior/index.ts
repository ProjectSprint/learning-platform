/**
 * Behavior domain exports.
 * Provides access to all behavior types and related utilities.
 */

export type {
	Behavior,
	BehaviorAnimation,
	BehaviorContext,
	BehaviorResult,
} from "./Behavior";
export { BaseBehavior, StubBehavior } from "./Behavior";
